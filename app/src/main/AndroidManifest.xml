<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="org.mpardalos.homeworkmanager"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="org.mpardalos.homeworkmanager.TaskList"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="org.mpardalos.homeworkmanager.TaskAdd"
            android:label="@string/title_activity_task_add"
            android:parentActivityName="org.mpardalos.homeworkmanager.TaskList">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.mpardalos.homeworkmanager.TaskList"/>
        </activity>
        <activity
            android:name="org.mpardalos.homeworkmanager.TaskEdit"
            android:label="@string/title_activity_task_details"
            android:parentActivityName="org.mpardalos.homeworkmanager.TaskList">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.mpardalos.homeworkmanager.TaskList"/>
        </activity>
    </application>

</manifest>
